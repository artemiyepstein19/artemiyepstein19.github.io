# Artemiy Epstein — Operations & Project Management

Static production build for [artemiyepstein.com](https://artemiyepstein.com).

The site is published with GitHub Pages from the `main` branch. The `CNAME`
file connects the custom domain, and `.nojekyll` preserves the generated
Next.js asset paths.
